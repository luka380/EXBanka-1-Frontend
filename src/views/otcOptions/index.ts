// Public surface of the OTC Options view module. Consumers should only
// import this file (the route + the view name) — everything else is
// internal to the module.
export { OtcOptionsView } from './OtcOptionsView'
