// Public surface of the Settings hub. Mount at `/admin/settings` with nested
// child routes for each panel (notifications, fees, peer-banks, roles, …).
export { SettingsView } from './SettingsView'
